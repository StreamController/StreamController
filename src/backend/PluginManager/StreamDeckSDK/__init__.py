"""
Support for plugins written against the Elgato Stream Deck SDK.

Plugins are installed as ``.sdPlugin`` directories under
``<data path>/streamdeck-plugins``. Each one is presented to the rest of
StreamController as a regular plugin so that its actions appear in the action
chooser and can be placed on pages like any other action.
"""
